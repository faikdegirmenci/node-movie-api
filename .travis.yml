language: node_js
node_js:
  - 10.16.1
before_install:
  - npm install
  - export NODE_ENV=production
  script:
  - npm run test

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: gh-pages